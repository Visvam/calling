<h1>Log in</h1>

    <%= form_for :sessions, :url => {:controller => "sessions", :action => "create"}  do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name%>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.submit "Log in" %>
    <% end %>

   